spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL
  sql:
    init:
      mode: always
      data-locations: data.sql
  h2:
    console: /h2-console
  shell:
    interactive:
      enabled: false
  main:
    allow-circular-references: true